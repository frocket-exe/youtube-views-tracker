name: Update Total Views

permissions:
  contents: write

on:
  schedule:
    - cron: "30 * * * *" 
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install google-api-python-client google-auth google-auth-httplib2 google-auth-oauthlib

      - name: Run script
        env:
          API_KEY: ${{ secrets.YT_API_KEY }}
          SEND_EMAIL: ${{ secrets.SEND_EMAIL }}
          LOGIN_EMAIL: ${{ secrets.LOGIN_EMAIL }}
          SEND_PW: ${{ secrets.SEND_PW }}
          RECEIVE_EMAIL: ${{ secrets.RECEIVE_EMAIL }}
        run: |
          python main.py

      - name: Commit and push results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add views/total_views.json
          git add views/yt_views.json
          git add views/yt-short_views.json
          git add views/ig_views.json
          git add views/tt_views.json
          git add viewData/milestones.json
          git add viewData/yearStartViews.json
          VIEWS=$(python -c "import json; print(json.load(open('views/total_views.json'))['totalViews'])")
          git commit -m "Update views → $VIEWS" || echo "No changes to commit"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
